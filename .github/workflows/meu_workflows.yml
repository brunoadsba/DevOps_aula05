name: Construindo e Testando

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: windows-latest
    steps:
      - name: primeiro-passo
        run: echo "Olá mundo DevOps"
